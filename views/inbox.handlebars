<br><br><br>
<div class="ui main text container">
	<h1>Inbox</h1>
	<p>You have {{inbox}} messages.</p>
	<table border = "1">
		{{#each row}}
		<tr>
			<td>{{this.firstname}}</td><td>{{this.message}}</td>
		</tr>
		{{/each}}
	</table>
</div>